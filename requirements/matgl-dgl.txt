-r matgl.txt

# Optional legacy DGL backend for MatGL models that still require DGL.
# Keep these pins together; MatGL upstream documents this combination for DGL.
torch==2.3.0
torchdata<=0.8.0
dgl==2.2.0
